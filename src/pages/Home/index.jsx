import styled from 'styled-components';
import Background from './background';

const Home = () => {
  return (
    <Wrapper>
      <Background />
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default Home;
